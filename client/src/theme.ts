import { DefaultTheme } from "react-native-paper";

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#4f8cff",
    accent: "#ffb300",
    background: "#f5f6fa",
    surface: "#fff",
    text: "#22223b",
    placeholder: "#a0aec0",
  },
};